
# find the pivot index
def partition(arr, l, r):
    return index


def kthLargest(arr, l, r, k):
    print('do something')
    # call parition function to find pivot index position

    # compare pivot index with k
    # if pivot index is equal to


# Driver Code
# declare an unsorted array
# declare k integer
# invoke function kthLargest
